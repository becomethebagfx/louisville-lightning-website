# Wiring Audit: PA Announcer Intros (2026-07-03)

Scope: walk-up announcer feature (commit e600262 + fix batch). New:
src/lib/wav.ts; changed: db.ts (announcement storage), useAudioPlayer.ts
(intro-then-song chaining + pregame sequence), EditPlayerModal.tsx
(record/upload/preview/remove), PlayerCard.tsx (badge), WalkUpPage.tsx
(preload, introIds, pregame button). External: 11 TTS intros (Gemini
Charon voice) uploaded through the live coach UI into the walkup-audio
bucket as `${id}.announce`.

## Outcome verification (before audit)
- Upload path: all 11 intros loaded through the production edit modal;
  "Pregame Intros (11)" + 11 card badges appeared after storage re-scan.
- Intro-then-song verified live by timing: Elijah's song was 5.9s into
  its 19.5s clip start when sampled ~12.7s after tap = 6.8s intro played
  first. Pregame mode advanced Micah -> Colt in lineup order; tap-to-stop
  halts the queue.

## Round 1 (2 agents: static, runtime/UX/copy)

Tier 1: none.

Tier 2 (2, fixed):
1. [UX] Save while recording silently discarded the take. FIX: Save
   guarded in handler + disabled while isRecording (which stays true
   through the async decode, closing the decode-window variant T3-3).
2. [Static] Intro preload re-probed Supabase (404s) for every intro-less
   player on every drag-reorder tick (players array identity dep). FIX:
   effect keyed on sorted id-set string; reorders no longer refetch.

Tier 3 (8, fixed or resolved):
- Cross-stop the two modal previews (clip vs intro). FIXED.
- "Not saved yet" hint on pending intro. FIXED.
- Pregame queue aborted on a single failed intro. FIXED: skips player
  (both missing-blob and play()-failure paths, requestId-guarded).
- Badge title-only affordance. FIXED: aria-label + role="img".
- makeElement defensive URL revoke. FIXED.
- Mid-pregame intro removal hit a non-null assertion. FIXED: guard+skip.
- recorder.onstop ran decode/setState after unmount. FIXED: onstop
  detached in cleanup (tracks still stopped).
- First-load pop-in of pregame button/badges (~1-2s on slow networks) and
  future 404 probes for intro-less players: documented as acceptable at
  team scale (probe design avoids any schema change).

Pre-existing, not a regression (documented): db.ts opens an IndexedDB
connection per call without close(); amplification reduced by the
id-set-key fix.

## Round 2 (fresh agent on the fix batch)
ZERO NEW FINDINGS. All fixes verified (guards, no infinite loops, no
stale closures, id round-trip lossless, lineup order preserved, a11y
correct). Build + eslint + em-dash sweeps clean. Diff limited to the 4
intended files.

## Verdict: CLEAN (audit clean, zero findings after fixes)
