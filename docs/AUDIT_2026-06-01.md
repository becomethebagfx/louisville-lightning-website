# Wiring Audit - 2026-06-01

## Phase: 7U season wind-down -> 8U & 9U tryouts update

### Scope
The 7U season is ending. Site rebranded to 8U & 9U travel baseball with a
dedicated tryouts page driven by the coaches' flyer. Theme unchanged. All
existing features preserved (walk-up songs, live scouting, roster).

### One Layer Up - every surface touched
| Surface | Change |
|---|---|
| `src/lib/tryoutData.ts` (NEW) | Single source of truth: dates, time, age rules, location, contacts, motto |
| `src/pages/TryoutsPage.tsx` (NEW) | Dedicated `/tryouts` page, full flyer detail |
| `src/App.tsx` | Added `/tryouts` route |
| `src/components/Navbar.tsx` | Added highlighted "Tryouts" link |
| `src/components/Hero.tsx` | Rebrand "7U" -> "8U & 9U"; pulsing tryouts banner; primary CTA -> `/tryouts` |
| `src/components/About.tsx` | Stat "7U / Born 5/1/2018+" -> "8U · 9U / Age Groups" |
| `src/components/Schedule.tsx` | Replaced finished 2026 tournament list with tryout dates/location + `/tryouts` CTA; coaches + team size kept (Josh Deacon retained) |
| `src/components/Contact.tsx` | Single Davis contact -> two age-group cards (Garvin/9U, Davis/8U) |
| `src/components/Footer.tsx` | Rebrand age line; added Tryouts link; dropped mislabeled Schedule anchor |
| `index.html` | SEO meta now mentions 8U/9U tryouts |

### Tryout data (verbatim from flyer + coach texts)
- 9U: Sun July 20, 4:00 to 6:00 PM. Cannot turn 10 before May 1 (born on/after May 1, 2016). Steven Garvin, 502.821.1880.
- 8U: Sun July 26 & Sun Aug 2, 4:00 to 6:00 PM. Cannot turn 9 before May 1 (born on/after May 1, 2017). Taylor Davis, 502.299.1804.
- Location: Watkins Church, 9800 Westport Road, Louisville, KY 40241.
- "Built on character. Focused on excellence."

### Verification gates
- **Code correctness**: `tsc -b && vite build` -> green (505 modules). My files lint-clean (7 eslint errors all pre-existing in scout/walkup/background files).
- **Wiring**: all 6 `/tryouts` links + route + page resolve; tryoutData consumed by Contact/Schedule/TryoutsPage (no dead exports after TRYOUT_MOTTO wired); no stale user-facing "7U"/"2018".
- **Rule 7**: em-dash/en-dash gate CLEAN across `src` + `index.html` (also scrubbed pre-existing dashes).
- **Visual (Playwright)**: 1440x900 + 375x812. Home hero, "THE DETAILS" (schedule), "GET IN TOUCH" (contact), `/tryouts` full page, and `/walkup` all verified rendering correctly. 0 console errors (1 pre-existing PWA meta deprecation warning). Screenshots in `screenshots/AFTER/`.

### Features preserved (confirmed live in preview)
- Walk-Up Songs (`/walkup`): player list, play buttons, speaker setup, add-to-home-screen - intact.
- Live Scouting (`/scout`): route + page untouched.
- Roster, Supabase wiring: untouched.

### Status: CLEAN
