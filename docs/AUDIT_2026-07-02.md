# Wiring Audit: 9U Program Removal (2026-07-02)

Scope: commit removing all 9U content + Steven Garvin site-wide (club is
8U only, two teams). One-layer-up surface map: tryoutData.ts 9U group,
Hero badge + banner, Schedule label + note, Footer tagline, About stat
tile, RegisterPage 9U notice, single-card layout guards on TryoutsPage +
Contact.

## Verification round (fresh agent on deployed state)

ZERO FINDINGS.

- Diff: no dangling refs; TRYOUT_GROUPS consumers handle single element;
  length>1 layout guards center the lone cards; RegisterPage imports
  trimmed cleanly.
- grep 9U/Garvin in src/, index.html, manifest.json: zero.
- Copy coherence: "age group" phrasing gone; Hero/Footer/Schedule/About
  all 8U-only; "TWO 8U TEAMS" heading coherent.
- Build PASS, eslint clean on all 8 changed files, em-dash zero.
- LIVE: /, /tryouts, /register HTTP 200; deployed bundle
  index-DvCeHj0U.js fetched and grepped: garvin=0, 9u=0 (with positive
  controls "Taylor Davis"/"Danny Knapp"/"8U Travel Baseball" present).
- Non-issues: "7U" hits are opponent names in scoutData; internal
  bornCutoff code comment; historical audit docs record pre-removal state.

## Verdict: CLEAN (audit clean, zero findings)
