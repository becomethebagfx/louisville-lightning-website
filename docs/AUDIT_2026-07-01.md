# Wiring Audit: Tryout Registration Feature (2026-07-01)

Scope: commit b159914 (Google Form embed at /register, Two Teams section,
head-coach roles, nav/footer Register links) + external wiring (Google Form
on loulightningclub@gmail.com -> "Louisville Lightning Tryout Signups 2026"
Sheet, live at loulightning.com/register).

Three parallel agents: A static-code, B runtime/UX/a11y, C copy/data/privacy.

## Round 1 findings and resolutions

### Tier 1
None. (Privacy sweep clean: no cost/dues, tournament dates, practice
weekdays, or unpublished coach names anywhere in src/. Em-dash sweep zero.
tsc + eslint clean. SPA rewrite covers /register in public/_redirects AND
render.yaml. Form + Sheet E2E proven by test submission.)

### Tier 2 (all fixed)
1. [C] 9U parents funneled into the 8U-only form with no 9U path.
   FIX: RegisterPage now states the form is 8U-only and routes 9U families
   to Steven Garvin (pulled from TRYOUT_GROUPS, single source).
2. [C] "TWO TEAMS / Louisville Lightning fields two teams" read club-wide
   but is 8U-only. FIX: heading "TWO 8U TEAMS" + scoped body copy.
3. [B] iframe fixed 3800px clipped the Submit button at <=360px widths
   (measured: form 4221px @320w, 3966px @360w, 3880px @375w, 3311px @740w;
   validation errors grow it further). FIX: responsive heights
   h-[4500px] sm:h-[4100px] md:h-[3700px], border via class.
4. [A] "You will get an email copy" depends on form receipts setting.
   VERIFIED ON (Settings: Collect email = Responder input, Send copy =
   Always; live form shows "A copy of your responses will be emailed").
   No code change needed.

### Tier 3 (all fixed)
1. [A] Schedule.tsx duplicated head-coach/team mapping. FIX: derived from
   LIGHTNING_TEAMS.
2. [A/C] RegisterPage `headCoach.split(' ')[1]` brittle + last-name-only
   inconsistent. FIX: full "Head Coach <name>" from data.
3. [A] iframe loading="lazy" on primary content. FIX: removed.
4. [A] Unencoded sms: body strings (TryoutsPage + pre-existing Contact).
   FIX: encodeURIComponent both.
5. [B] Navbar overflow at 320px (logo shrank to 0). FIX: flex-shrink-0 on
   logo, px-2 sm:px-3 + py-2.5 links (also fixes <44px tap-target finding).
6. [B] Footer muted text under WCAG AA (white/40 ~3.85:1, white/30 ~2.75:1
   on navy-900). FIX: bumped to white/60 / white/50; footer links +py-1.
7. [B] No visible keyboard focus on clip-path CTAs (clip-path clips
   outlines AND box-shadows). FIX: inset :focus-visible outlines
   (negative outline-offset) inside the clipped shape.
8. [B] Decorative SVGs lacked aria-hidden. FIX: aria-hidden="true" added to
   26 decorative svgs across Navbar/Footer/Schedule/Contact/Hero/About/
   TryoutsPage (RegisterPage has none).
9. [C] "takes about 2 minutes" undersold a 14-field form. FIX: "takes a
   few minutes".
10. [C] "11 players on the roster" stale with two 8U teams. FIX: "11
    players per team".
11. [C] Age-cutoff dates (born on/after May 1 2017/2016) are pre-existing
    site facts, internally consistent, but unverified against the coaches'
    letter. OPEN QUESTION flagged to Brandon (no code change; data
    predates this feature).

## Round 2 (fresh agents on post-fix tree)
- Static agent: ZERO NEW FINDINGS. All 8 round-1 fixes verified correct;
  arbitrary Tailwind heights present in built CSS (not purged); focus-ring
  CSS vars resolve; aria-hidden sweep left no interactive element nameless
  (Instagram link keeps its aria-label); eslint errors exist only in 5
  files untouched by this changeset (pre-existing debt: LightningBackground,
  LiveScoutInput, InstallBanner, useRoster, useScoutObservations).
- Runtime agent re-verify: iframe Submit reachable at 320/375/640/768+
  (measured vs each height bucket); footer contrast passes; focus rings
  render; no horizontal overflow at 320-1440; teams line wraps to 2 clean
  lines at 375px; 9U notice renders with working tel: link.
- UX/copy agent: ONE new Tier 3: Hero banner "Tryouts Now Open · 8U & 9U"
  overstated 9U (no 9U date exists). FIXED: banner now "Now Recruiting ·
  8U & 9U" (matches Schedule.tsx framing). Everything else clean: privacy
  re-sweep zero, numbers consistent, console errors only from Google's own
  fonts inside the form iframe.

## Round 3 (scoped: blast radius of the round-2 fix = one string)
- grep "Now Open" in src/: zero. Build: PASS. Em-dash: zero.
- No other surface references tryout "openness" phrasing; /tryouts and
  Contact already carry the accurate TBD/contact framing.
- ZERO findings. Two consecutive clean rounds on all non-trivial scopes.

## Verdict: CLEAN

Open items for Brandon (data questions, not defects):
1. Publish Beau Schoenbaechler as Blue co-coach? (In Taylor's letter;
   withheld pending approval.)
2. Confirm age-cutoff birth dates (born on/after May 1, 2017 for 8U /
   May 1, 2016 for 9U) against the coaches' rules.
3. Optional: Apps Script auto-close of the form after July 26 (needs one
   OAuth Allow click); droplet SMS reminder for July 27 is already
   installed either way.
- Screenshots: screenshots/AFTER/ (round 1 + round 2 sets, 1440 + 375).

## Gates
- npm run build: PASS (post-fix)
- eslint on changed files: PASS (only pre-existing set-state-in-effect
  warning in useScoutObservations.ts, documented June 2026)
- grep -rnE em/en dash in src/: ZERO
- Live checks: loulightning.com/{,tryouts,register} HTTP 200; form
  viewform + ?embedded=true HTTP 200
